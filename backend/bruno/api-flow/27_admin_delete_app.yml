info:
  name: "27 Admin Delete App"
  type: http
  seq: 27

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/applications/delete-app"
  auth:
    type: bearer
    token: "{{adminToken}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": {{adminAppId}}
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("admin can delete application from system", function() {
          expect(res.status).to.equal(200);
          expect(res.body).to.be.an("object");
          expect(res.body.result).to.equal("deleted");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5


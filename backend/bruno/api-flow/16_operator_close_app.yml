info:
  name: "16 Operator Close App"
  type: http
  seq: 16

http:
  method: PUT
  url: "{{baseUrl}}/api/operator/applications/close-app"
  auth:
    type: bearer
    token: "{{operatorToken}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "id": {{mainAppId}},
        "comment": "{{operatorCloseComment}}"
      }

runtime:
  scripts:
    - type: tests
      code: |-
        test("operator can close application", function() {
          expect(res.status).to.equal(200);
          expect(res.body).to.be.an("object");
          expect(res.body.result).to.equal("closed");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
